Ponytail full review: the release reuses the existing GitHub Release workflow, adds
no dependency or abstraction, uses a patch version, and changes only required
metadata, current installation docs, tests, and version-sensitive fixtures.
