Ponytail review applied: use a patch release, keep the existing release workflow,
add no dependency, and change only version metadata, current documentation, and
the fixture literals required by the version bump.
